//WXS（WeiXin Script）是小程序的一套脚本语言，结合 WXML，可以构建出页面的结构。

/*
WXS 与 JavaScript 是不同的语言，有自己的语法，并不和 JavaScript 一致。
  * 不能使用ES6语法
  * 只能引入wxs文件
*/

var username = 'laoxioe';

// console.log(window)

module.exports = {
  username:username
}